# Repository Guidelines

## Project Structure & Module Organization
- `sm_td/`: Core C module for QMK (`sm_td.c`, `sm_td.h`, `qmk_module.json`).
- `tests/unit/`: Python `unittest` suites — compile and call the C library via `ctypes` and `clang` (see `tests/unit/sm_td_bindings.py`). Feature folders (e.g., `caps_word_enable/`) contain `layout.c` and `test.py`.
- `tests/integration/`: QMK-native googletest suites — compile `sm_td.c` against real `qmk_firmware` checkout. Suites in `suites/smtd_*/`, scripts `fetch.sh`/`run.sh`.
- `tests/run_tests.py`: Python test runner (discovers tests in `tests/unit/`).
- `docs/`: User docs and customization guides.
- `keyboards/`: Example QMK integration assets.

## Architecture
- Entrypoint: `process_smtd(...)` interprets tap vs. hold and schedules deferred actions.
- Hooks: implement `on_smtd_action(...)` and configure `SMTD_GLOBAL_*` macros for timing/features.
- Tests: Python drives C via `ctypes`; timeouts simulated with `prolong()` in key helpers (event-driven) or with the virtual clock `smtd.wait(ms)` (time-driven, required for dynamic timeouts).
- Key output ("pipeline taps", since #23 fix): `SMTD_TAP_16`/`SMTD_REGISTER_16`/`SMTD_UNREGISTER_16` call `smtd_tap_code16`/`smtd_register_code16`/`smtd_unregister_code16`. When the sent key equals the keymap keycode at the pressed position (and `use_cl != false`), the key goes through `smtd_emulate_key` -> full `process_record()` pipeline (Caps Word, combos, etc. see it); otherwise a manual `process_caps_word()` shim runs before a direct `tap_code16` send. Controlled by `SMTD_GLOBAL_PIPELINE_TAPS` (default true) and per-key `SMTD_FEATURE_PIPELINE_TAPS`.
- `smtd_executing_state` tracks the state whose action is running (set in `smtd_execute_action`); `state->emulated_register` pairs an emulated register with an emulated release even if the layer changed in between. `smtd_emulate_key` saves/restores `smtd_bypass` because it can be called while bypass is already set.

## Build, Test, and Development Commands
- `just test` — all tests (python + qmk native)
- `just test python` — python unit tests only
- `just test qmk` — all qmk native suites against default QMK version
- `just test qmk full` — specific suite (smtd_full); prefix `smtd_` is auto-added
- `just test qmk 0.32.16` — all suites against a specific QMK version
- `just test qmk 0.32.16 full` — specific version + suite
- `just test qmk-matrix` — all pinned QMK versions × all suites
- `just configure` — set QMK checkout directory (saved in `.env`, gitignored)
- `just fetch-qmk` / `just fetch-qmk 0.32.16` — download QMK without running tests
- Direct python: `python3 tests/run_tests.py`, `python3 -m unittest tests.unit.caps_word_enable.test`
- Direct qmk native: `sh tests/integration/run.sh 0.33.5 smtd_full` (respects `SMTD_QMK_DIR` env var)
Notes:
- First `just test qmk` prompts for QMK checkout directory and saves it to `.env`.
- Python tests auto-compile a per-suite shared library with `clang -shared -fPIC` (macOS: `.dylib`, Linux: `.so`). No manual `cmake` step is needed.
- QMK native tests need `make` and a C++ toolchain (QMK's googletest harness).
- Prereqs: Python 3.10+, `clang` on PATH, `just` (https://github.com/casey/just).

## Coding Style & Naming Conventions
- C11 for `sm_td`; Python for tests.
- C: snake_case for functions (`process_smtd`), ALL_CAPS for macros (`SMTD_*`), K&R braces, 4-space indents (prefer spaces in new code).
- Headers: keep QMK includes behind guards; use `static` for internal symbols; prefix public API with `smtd_`/`SMTD_`.

## Testing Guidelines
- Framework: Python `unittest` driving the C library via `ctypes`.
- Structure: each feature folder has `layout.c` and `test_*.py`; use helpers in `tests/unit/sm_td_assertions.py`.
- Naming: discovered by `test*.py`; keep scenario names descriptive.
- Determinism: seed randomness in tests that sample (e.g., `random.seed(…)` in `setUp`) to stabilize failures.
- Known baseline failures (pre-existing): `test_stirred_long_mod_smtd_press2_fixed` in `caps_word_enable` and `complex_layout` (4 FAILs total) - prolong/defer assertion issue, unrelated to recent work.
- Mocks (`tests/unit/sm_td_bindings.c`) mirror QMK essentials, including Caps Word state and weak mods; suites with `CAPS_WORD_ENABLE` must define `caps_word_press_user` in their `layout.c`. History `mods` field records `current_mods | weak_mods`; emulated `process_record` events are recorded with `keycode=65535` plus row/col.
- macOS dylib linking rejects `__attribute__((weak))` *undefined* function references - use `#ifdef` selection instead of weak defaults inside the single-TU test build.
- Virtual clock (since #45 work): `mock_time_ms` in `tests/unit/sm_td_bindings.c` backs `timer_read32`/`timer_elapsed32`; `defer_exec` records `deadline_ms`; `TEST_advance_time(ms)` / Python `smtd.wait(ms)` advances time and fires due deferred execs in deadline order. The clock stands still unless `wait()` is called, so legacy `prolong()`-driven tests are unaffected. NB: `CDeferredExecInfo` in `sm_td_bindings.py` must mirror the C struct layout exactly (array stride), update both together.
- One test.py may load several layouts (`load_smtd_lib` hashes the path per dylib) - see `tests/unit/dynamic_release/test.py` with four layouts: ratio 5 (SMTD_MT + SMTD_LT + per-key release-term cap), ratio 0 fallback, ratio 1 clamp, and SMTD_ENABLE_QMK_TAPHOLD with raw MT()/LT() under dynamic timings. Also covers 3-key rolls (each state takes p1/p2 from its immediate follower) and multi-tap sequences (p1 measured from the latest touch; sequence expiry resets tap_count).
- `layout_fixed.c`/`layout_ratio1.c` are sed-generated snapshots of `layout.c` (see git history for the command) - regenerate them when editing `layout.c`.

## Real-Hardware Testing (owner's setup)
- Firmware repo: `../qmk_firmware_zsa` (keyboard `zsa/voyager`, keymap `sm`); `modules/stasmarkin/sm_td/` there is symlinked to this repo's `sm_td/`, so builds pick up local changes directly.
- Build/flash: `cd ../qmk_firmware_zsa && just build` / `just flash` (DFU). That keymap enables CAPS_WORD, COMBO, REPEAT_KEY, UNICODE - a good integration stress test.

## Chordal Hold (SMTD_CHORDAL_HOLD)
- QMK-style "opposite-hands rule": a tap-hold settles as HOLD only when an opposite-hand key is involved; same-hand rolls stay taps. Enable with `#define SMTD_CHORDAL_HOLD 1` in `config.h`; default 0 compiles out entirely (zero code/RAM when off, all `#if`-guarded).
- Handedness comes from `char smtd_chordal_handedness(keypos_t)` (declared in `sm_td.h`). Default (weak) reads a user-supplied `const char chordal_hold_layout[MATRIX_ROWS][MATRIX_COLS]` from PROGMEM using QMK's `'L'`/`'R'`/`'*'` (left/right/neutral-thumb) convention. Keymaps may override the weak function to compute handedness without the array.
- Design is **deferred**, fitting sm_td's permissive-hold-on-release model (not decide-on-press like QMK): on a following PRESS nothing settles, a same-hand press only `cancel_deferred_exec`s the hold timeout; TAP/HOLD is decided when a key is RELEASED via `smtd_chordal_all_same_hand()` scanning live states. Because every pressed key (even plain ones) owns a `smtd_state`, the opposite-hand key is visible to that scan.
- Hooked into `SMTD_STAGE_TOUCH` (press cancels same-hand timeout; release runs the hand scan) and `SMTD_STAGE_TOUCH_RELEASE` (release runs the hand scan), all under `#if SMTD_CHORDAL_HOLD`.
- Thumbs (`'*'`) are neutral: `all_same_hand('*')` returns false, so thumb tap-holds keep the default hold-capable behavior regardless of the following key's hand.
- Tested two ways: Python mocks `tests/unit/chordal_hold/` (mock defines `pgm_read_byte`; the array path) and QMK-native `tests/integration/suites/smtd_chordal_hold/` (real pipeline + real PROGMEM `chordal_hold_layout`, 4x10 matrix, handedness by row). Cover same-hand roll→tap, cross-hand→hold, timeout cancel/keep, two same-hand mod-taps ±cross-hand key, layer-tap, thumbs, and tap regressions. QMK-native caveat: sm_td's hold is triggered by the following key (not the idle timeout), and resolved emulation flushes when the tap-hold key itself is released — so those tests assert the full report sequence with one `VERIFY_AND_CLEAR` after that release.

## Known Issues & Backlog
- Divergence from QMK (intentional, YAGNI): real QMK turns Caps Word off when a non-shift mod-tap is held; sm_td's MT hold (`register_mods`) stays invisible to Caps Word.
- `fixme-sm` in `smtd_emulate_key`: emulating keycodes absent from the keymap could use combo-style records (`record->keycode`, requires `COMBO_ENABLE`/`REPEAT_KEY_ENABLE`).
- Issue #45 (dynamic timeouts) IMPLEMENTED (2026-06-13, unreleased): `smtd_compute_release_term` derives the touch-release window as `min(p1, p2) / SMTD_GLOBAL_RELEASE_RATIO` (default 5), clamped to [1ms .. per-key SMTD_TIMEOUT_RELEASE]; ratio 0 restores the fixed timeout. Owner explicitly chose the min(p1,p2) "AND" rule (hold iff p3 << p1 AND p3 << p2): in rolls the release rhythm mirrors the press rhythm (p3 ~= p1), so p1 acts as a roll veto. Accepted trade-off: the combo-like case `p1 ~= p3 << p2` resolves as tap-tap (switching to a p2-only rule = one-line change in that function). PR #54 attempted this and is broken - do not merge.
- Useful: `gh issue view <n> --repo stasmarkin/sm_td --comments` to read issue threads.
- Issue #40 (VIA/Vial support) research (2026-06-13): with `SMTD_ENABLE_QMK_TAPHOLD` sm_td already works with VIA/Vial-remapped MT()/LT() — dynamic keymap stores plain 16-bit keycodes and overrides `keymap_key_to_keycode()` (which sm_td.c uses for stack resolution), so GUI remaps are picked up with no recompile. Custom sm_td keycodes must be based at `QK_KB_0` (0x7E00, 32 named slots) + a per-keyboard `customKeycodes` array in via JSON / vial.json to show names in the GUI (SAFE_RANGE/QK_USER codes still work but display as hex via the "Any" key). Runtime timeout config would need SMTD_GLOBAL_* `#define`s converted to runtime vars + VIA v3 custom menu (`via_custom_value_command_kb`, per-keyboard JSON); Vial's QMK Settings tab is NOT third-party extensible (requires upstreaming into vial-qmk + vial-gui, as Getreuer did for Chordal Hold in Vial 0.7.4). No community tap-hold lib has GUI integration today; Vial's own dynamic Tap Dance is admitted by Vial docs to be poor for HRM.
- QMK-native testing research (2026-06-13, deep-research + local exploration of `../qmk_firmware_zsa`): integrating sm_td into QMK's REAL googletest harness is feasible with an additive overlay, no QMK fork needed. Key facts: (a) `builddefs/testlist.mk` auto-discovers full tests via `find tests/ -name test.mk`, so dropping `tests/<name>/{test.mk,config.h,*.cpp}` into a qmk_firmware checkout registers a suite; (b) `test.mk` can do `SRC += .../sm_td.c` + `VPATH +=` — `build_full_test.mk` compiles it together with the REAL `$(QUANTUM_SRC)` (action.c, action_tapping.c, caps_word, deferred_exec) into a native host gcc/g++ executable (`PLATFORM:=TEST`, GDB-debuggable); (c) test API: `TestFixture` (`set_keymap`, `idle_for(ms)`, `run_one_scan_loop`), `KeymapKey(layer, col, row, kc)` (NB: col before row), gmock `TestDriver` + `EXPECT_REPORT` asserts real HID reports; closest precedent: `tests/tap_hold_configurations/chordal_hold/` (Getreuer's PR #24560); (d) BLOCKER: community-module codegen (`community_modules.c/h`, introspection) lives only in `build_keyboard.mk` — `build_test.mk` has zero module support, so module hooks (`*_sm_td` callbacks via qmk_module.json) won't auto-wire in tests; needs manual glue/stubs or calling `process_smtd` directly from a test shim; (e) docs.qmk.fm/unit_testing is stale (~2017 text \"full integration test not yet possible\" + references to pre-builddefs file layout) — trust repo code, not docs; (f) CI matrix over QMK versions = checkout tags + symlink overlay into `tests/` + `make test:<name>`; test_common API drifts across versions (KeymapKey/chordal-hold era changes), pre-0.28 tags have no module system at all; (g) no known precedent of a community module tested inside the QMK harness — sm_td would likely be first.
- Layer-issue audit of release-0.6.2 (2026-06-15): the single commit (`LAYER_PUSH/LAYER_RESTORE` macros → native `layer_on`/`layer_off`; old macros did `layer_move`, replacing the whole layer mask, and tracked a single saved return layer). Net effect: SMTD_LT is now additive and no longer wipes foreign layer bits on release. **Issue #57** (TG/TO toggled inside an SMTD_LT held layer was dropped on release) is genuinely fixed. **Issue #44** (tri-layer) is unblocked: two SMTD_LT layers can now coexist so a `layer_state_set` tri-layer hook can fire. **Issue #47** (Layer Lock) is NOT addressed by this change — it's a QMK feature handled in `process_record`, and the thread (#47) points at tapping-term/RETRO_TAPPING, not `layer_move`. **Issue #56** (OSL) is NOT fixed: owner confirmed in-thread that sm_td has no OSL support at all (user worked around it with `SMTD_DANCE` + `set_oneshot_layer`). Regression tests for #57 and #44 added in `tests/unit/layer_features/` (red on 0.6.1 commit 6684a52, green on 0.6.2). #47/#56 are not faithfully testable in the ctypes harness (their QMK logic would live in the mock layout, so a test would validate the mock, not sm_td).
- Test harness: modeling QMK layer features. `tests/unit/sm_td_bindings.c` routes `layer_move`/`layer_on`/`layer_off` through `layer_state_set_user(uint32_t)` (mirrors QMK's hook chain where tri-layer/layer-lock adjust the mask). Default is identity; a layout opts in by `#define SMTD_LAYOUT_DEFINES_LAYER_HOOK` before the `#include "../sm_td_bindings.c"` and defining its own `layer_state_set_user` (see `tests/unit/layer_features/layout.c`, tri-layer L1+L2→L3). Same `#ifdef`-selection idiom as the weak-symbol caveat above (single-TU build forbids a weak default + override). A `TG()`-style plain key is modeled in `post_process_record` (look up keycode via `keymap_key_to_keycode(get_highest_layer(layer_state), …)`, toggle the layer on `pressed` only). NB: `TEST_get_layer_state()` returns `get_highest_layer` (a single layer index), not the raw mask — assert on the highest active layer.
- CAVEAT for SMTD_ENABLE_QMK_TAPHOLD on real firmware (2026-06-13, unverified on hardware): QMK's own action_tapping intercepts MT()/LT() BEFORE process_record_user (quantum/action.c:133-134 — pre_process_record_quantum, then action_tapping_process; process_record(&tapping_key) fires only after QMK's own tap/hold resolution). So sm_td receives MT/LT events time-compressed and pre-resolved: release-rhythm timing is destroyed, holds get double latency (TAPPING_TERM + sm_td TAP_TERM). Tests don't catch this (bindings call process_record directly, no action_tapping mock); owner's voyager keymap has no MT()/LT(). Fixes: document `NO_ACTION_TAPPING` as a requirement (action.c then calls process_record directly), or intercept MT/LT in pre_process_record. Hook history: module registered on `pre_process_record_sm_td` in v0.5.1 (ba8801a, "sm_td as qmk module"); renamed to `process_record_sm_td` in v0.5.5 (1aa47b7, 2026-01-17) — no reason in commit/issues. Reconstructed reason: align module-hook stage with manual install (which lives in `process_record_user`) so emulated taps (process_record re-runs) share one pipeline — became load-bearing for v0.5.6 pipeline taps / Caps Word (docs/030_known_problems.md:46). KEY INSIGHT: input-capture stage and tap-emit stage are independent — a minimal MT/LT-only module can READ on pre_process_record (raw timing, consumes before action_tapping) and EMIT via process_record (pipeline). The v0.5.5 parity reason does NOT apply to a minimal module (no user on_smtd_action/process_record_user to stay in sync with). Gate before building: hardware test on voyager (add LT()/MT(), TAPPING_TERM 200, watch hold latency; toggle NO_ACTION_TAPPING) to confirm action_tapping actually breaks timing and pre_process fixes it.
- QMK-native test harness PROTOTYPE SHIPPED (2026-06-13, unreleased) in `tests/qmk_native/`: compiles `sm_td.c` against a REAL, separately-cloned `qmk/qmk_firmware` (default tag 0.33.5) — real `action.c`/`action_tapping.c`/`deferred_exec.c` in one native googletest binary, no fork. Layout: `overlay/<suite>/{test.mk,config.h,smtd_hooks.c,test_*.cpp}`; `fetch.sh` shallow-clones into gitignored `checkouts/<ref>/`; `run.sh [<ref>] [<suite>...]` COPIES the overlay into `checkouts/<ref>/tests/<suite>/` (NB: must be a real dir + real files — QMK's `find tests -type f -name test.mk` does NOT follow dir-symlinks nor match symlinked files) and symlinks sm_td sources as `smtd_src/` (referenced by VPATH, not find). Version matrix = `run.sh <other-tag>` → separate `checkouts/<ref>/`. Wiring: `test.mk` does `DEFERRED_EXEC_ENABLE=yes`, `OPT_DEFS += -DQMK_KEYBOARD_H=\"quantum.h\"` (sm_td.h `#include QMK_KEYBOARD_H` has no keyboard in a test build), `SRC += sm_td.c smtd_hooks.c`. NO `SMTD_UNIT_TEST`, NO `sm_td_bindings.c` — real QMK provides timer/register_code16/defer_exec/etc; assert via `EXPECT_REPORT` on real HID. `smtd_hooks.c` routes the real pipeline into sm_td via weak `process_record_user` -> `process_smtd` (same pipeline stage as the `process_record_sm_td` module hook), and MUST define `get_smtd_timeout`/`smtd_feature_enabled` (weak in sm_td.h; macOS ld rejects undefined-weak refs in an executable — same issue as the dylib note). Keymap is set per-test via `set_keymap({...})`; harness overrides `keymap_key_to_keycode` (test_fixture.cpp:38) which sm_td.c uses, so no `keymaps[][]`/`INTROSPECTION_KEYMAP_C` needed. INTER-TEST RESET REQUIRED (solved): sm_td global state (`smtd_states_pool`/`smtd_active_states`/timeouts) is NOT reset between `TEST_F` runs — the QMK fixture resets only QMK state — so a lingering SEQUENCE-stage state at a matrix position leaks its `desired_keycode`/stage into a later test pressing the same position. Fix SHIPPED: new public `smtd_reset()` in sm_td.c/.h (cancels each pooled state's deferred token, `reset_state`s the pool, zeroes active list + executing/bypass), called from each fixture's `SetUp()` via `extern "C" void smtd_reset(void);`. CAUTIONARY FINDING: before `smtd_reset`, the state leak MASQUERADED as a behavioral bug — a 1ms MT tap looked like it emitted `[KC_LCTL]` (mod) instead of `KC_A`, which was wrongly read as "action_tapping CAVEAT confirmed". With reset in place, BOTH suites are 5/5 GREEN and produce IDENTICAL reports: `smtd_qmk_taphold` (action_tapping ON) and `smtd_qmk_taphold_no_action_tapping` (`#define NO_ACTION_TAPPING`) both emit `KC_A` on MT tap, mod+key on hold, two letters on roll. So these COARSE functional scenarios do NOT reproduce the SMTD_ENABLE_QMK_TAPHOLD action_tapping divergence — action_tapping ON vs OFF is behaviorally identical here. The caveat (if real) is about HOLD LATENCY and roll/release RHYTHM, not tap-vs-hold correctness; reproducing it needs finer tests (measure WHEN the hold mod registers, compare press-to-mod latency ON vs OFF, multi-key roll timing) — not yet written. Net for this iteration: integration is PROVEN (real QMK pipeline, 5/5 green both configs); the action_tapping caveat remains UNVERIFIED by tests (earlier "confirmed" was a state-leak false positive). THIRD SUITE `smtd_minimal` (added same day): tests the `sm_td_minimal` module by compiling `sm_td_minimal.c` (which `#include`s `../sm_td/sm_td.c`) and wiring it via weak `pre_process_record_user` -> `process_smtd`, mirroring the module's `pre_process_record_sm_td_minimal` hook (BEFORE action_tapping). action_tapping stays enabled; on_smtd_action comes from the module's weak default (not redefined in the suite's smtd_hooks.c). Relative include resolves because `run.sh` symlinks BOTH repo dirs as siblings inside the suite dir (`smtd_min/`->`sm_td_minimal/`, `sm_td/`->`sm_td/`), and the test TU is found via `VPATH += $(TEST_PATH)/smtd_min`. Result: 5/5 green — the zero-config pre_process design compiles and behaves correctly against the real pipeline with action_tapping on. (Still a coarse functional check, not a hold-latency/rhythm measurement.) COMPREHENSIVE SCENARIOS ADDED (2026-06-13, same day): now FIVE suites, 36 tests total against qmk_firmware 0.33.5: `smtd_qmk_taphold` (5), `smtd_qmk_taphold_no_action_tapping` (5), `smtd_minimal` (9: tap/hold+key/right-hand-mod 5bit->8bit/LT layer/multi-tap/3-key-roll/plain), `smtd_full` (13) and `smtd_dynamic` (4). `smtd_full` uses a rich `on_smtd_action` (in its smtd_hooks.c) on real keycodes: `SMTD_MT`(KC_A=LCtrl, KC_J=LAlt), `SMTD_MTE`(KC_S=LShift eager), `SMTD_LT`(KC_D=layer1), `SMTD_TD`(KC_F/ESC); covers tap/hold+following-key/tap-then-hold/multi-tap/eager-mod/layer push+restore/external (non-sm_td KC_LSFT) mod/roll; `SMTD_GLOBAL_RELEASE_RATIO 0` (fixed) so a fast roll there resolves HOLD-TAP. `smtd_dynamic` uses `RELEASE_RATIO 5` and proves the rhythm rule: same MT/LT roll = TAP-TAP when release gap mirrors press gap, HOLD-TAP when releases are near-simultaneous (passed first try — dynamic release behaves as designed through the real pipeline). LIB ISSUES SURFACED while writing these: (1) `SMTD_TK`/`SMTD_TTO` multi-arg macro chains expanded to the wrong `_ON_MKEY` arity — FIXED (2026-06-13) in sm_td.h: the non-MKEY forms wrongly duplicated `key` into `(macro_key, tap_key)` like SMTD_MT, but TK/TTO's 2nd arg (tap_key / layer) is already distinct from the trigger, so no duplication is needed. `SMTD_TK2/3/4` now call `SMTD_TK{2,3,4}_ON_MKEY(key, tap_key[, threshold[, use_cl]])`; `SMTD_TTO2/3/4` likewise (TTO3 also previously dropped its threshold arg). Now compile and behave; covered green in `smtd_full` (tk_fires_tap_key_after_threshold, tto_moves_layer_after_threshold). A NEW sixth suite `smtd_caps_word` (CAPS_WORD_ENABLE=yes + standard caps_word_press_user) confirms sm_td pipeline taps are visible to the real process_caps_word (MT letter tap + plain letter get the weak shift and keep Caps Word on; space ends it). NB: under Caps Word the weak shift lingers in the report (`()[KC_LSFT]`) after the key until the next key / teardown clears it — assert the 3 reports `(LSFT),(LSFT,key),(LSFT)`, not a trailing empty. TOTAL NOW: 6 suites, 41 tests (5+5+15+9+4+3), all green on 0.33.5; Python engine suites unaffected by the macro fix (117 tests, only the 4 known baseline FAILs).
- COVERAGE EXPANDED (2026-06-14) to 9 suites, ~57 active tests + 3 DISABLED crash repros: added `smtd_external_mods` (ext plain KC_LSFT + sm_td MT(LCtrl): compose on a key, all 6 release-order permutations return clean via get_mods()==0), `smtd_dynamic` grown to 9 (combo-like, instant-overlap clamp, per-key release cap via get_smtd_timeout returning 5ms for KC_F, 3-key MT roll), plus `smtd_dynamic_fixed` (ratio 0: within/after 50ms window) and `smtd_dynamic_clamp` (ratio 1: upper-clamp at 50ms). `smtd_caps_word` grown to 5 (use_cl=false hidden-from-CapsWord tap unshifted; MT-hold of a non-shift mod BREAKS Caps Word and sends the key unshifted — SAME as native QMK, so the old AGENTS "MT hold stays invisible to Caps Word" divergence note is WRONG for this path). `smtd_full` grown to 16 (+LT+MT compose, faceroll). NB on Caps Word reports: the weak shift lingers as `()[KC_LSFT]` after a key until the next key — assert 3 reports, not a trailing empty.
- VIA/Vial dynamic-keymap suite `smtd_via` ADDED (2026-06-15) — 9th suite, contour now 9 suites / 56 active tests green on 0.33.5. Proves sm_td resolves keycodes LIVE from a REAL VIA/Vial dynamic keymap (so GUI remaps are picked up with no recompile). KEY FACTS: sm_td resolves via `keymap_key_to_keycode` (sm_td.c:993, `smtd_current_keycode`) LAZILY — once per state on first action, cached in `desired_keycode` (sm_td.c:778-779), cleared on `reset_state`; it never reads the static `keymaps[][]` directly. Real QMK dynamic chain: `keymap_key_to_keycode` (weak, keymap_common.c) → `keycode_at_keymap_location` (NON-weak override in dynamic_keymap.c:85) → `dynamic_keymap_get_keycode` → nvm → emulated EEPROM (`platforms/test/eeprom.c`, RAM buffer). Write API: `dynamic_keymap_set_keycode(layer, row, col, kc)`. BLOCKER + FIX (Option B): the harness defines `keymap_key_to_keycode` NON-weak (test_fixture.cpp:38) and short-circuits to its `set_keymap` vector via `get_keycode` BEFORE the dynamic chain — so out of the box dynamic keymap is untestable. `run.sh` now applies two idempotent, upstream-safe guards to the gitignored checkout (inert for all other suites): (1) wrap the harness override in `#ifndef SMTD_USE_DYNAMIC_KEYMAP` so a suite can compile it out and supply its own; (2) `#ifndef` guard around `platforms/eeprom.h`'s test-harness `TOTAL_EEPROM_BYTE_COUNT` (default 32, far too small) so a suite can raise it. NOT weak-ified — wrapping avoids two competing weak defs of `keymap_key_to_keycode` (harness + QMK core). suite WIRING: `test.mk` sets `DYNAMIC_KEYMAP_ENABLE=yes` + `SEND_STRING_ENABLE=yes` and `OPT_DEFS += -DSMTD_USE_DYNAMIC_KEYMAP -DDYNAMIC_KEYMAP_ENABLE -DDYNAMIC_KEYMAP_LAYER_COUNT=2 -DNUM_ENCODERS=0 -DTOTAL_EEPROM_BYTE_COUNT=1024`. DO NOT `SRC += dynamic_keymap.c nvm_dynamic_keymap.c` — `DYNAMIC_KEYMAP_ENABLE=yes` already pulls them into the full-test build; adding them = 24 duplicate-symbol link errors. `smtd_hooks.c` provides the dynamic-backed `keymap_key_to_keycode(layer, key) { return keycode_at_keymap_location(layer, key.row, key.col); }` (keypos_t is `{.col,.row}`; dynamic API is `(layer,row,col)`). TEST MECHANICS: `set_keymap()` is NOT used — `KeymapKey::press()` injects straight into the matrix via `press_key(col,row)` (test_keymap_key.cpp), so a `KeymapKey` is just a position holder and its keycode field is ignored; each test writes the live cells with `dynamic_keymap_set_keycode` in SetUp/body and zeroes the whole dynamic keymap to KC_NO in SetUp (no eeconfig_init needed — set/get hit the EEPROM buffer directly). 5 tests: dynamic keycode resolved, plain passthrough, live remap of same cell changes emitted keycode, dynamic mod-tap holds mod on a (dynamic) following key, remap mod-tap→plain drops the hold. NB: a HELD plain key stays in the report until release (so remap-to-plain hold sequence is `(F),(F,B),(F),empty`, not `(F),(F,B),(B),empty`). This is the first community-module test of the QMK dynamic-keymap path. Lower-fidelity alternatives considered + rejected: A=behavioral proxy via set_keymap live-remap (no real dynamic_keymap.c); C=round-trip mirror (no patch, but sm_td still reads the harness vector); D=Python/ctypes. Owner picked B (real path + patch) for max fidelity.
- ★★ CRASH RESOLVED (2026-06-15) — NOT an sm_td bug; a TEST-HARNESS ARTIFACT. The 3 `DISABLED_*_segfaults` tests were written WITHOUT a `TestDriver driver;`. `TestDriver`'s ctor does `host_set_driver(&m_driver)` where `m_driver` is a member of the (stack-local) driver object; the only driver constructed for these tests was the throwaway local in `TestFixture::SetUpTestCase()`, which is destroyed when that function returns. So QMK's `host.c` `driver` pointer DANGLED at a freed stack frame, and every scan `keyboard_task → led_task → host_keyboard_leds() → (*driver->keyboard_leds)()` dereferenced reused stack and jumped through a garbage function pointer (lands in gtest `Test::Run`/`os_stack_trace_getter`, `this`/value `0x0a` is just stale stack, NOT `SMTD_POOL_SIZE`). The 16 passing tests each declare their own `TestDriver driver;` (live for the test body), which re-registers a valid driver — that's the only reason they survive. This explains every symptom the prior investigation chased: opt-dependence (freed-stack contents shift with layout), ASan "DEADLYSIGNAL BUS" (wild jump through a dangling pointer, no adjacent OOB to report), and the multi-key correlation (those tests just happened to omit `TestDriver`). DIAGNOSIS METHOD: lldb instruction-step from `host_task` caught the jump origin: `host_keyboard_leds` → garbage; `p driver` showed a STACK address whose `keyboard_leds` field held a Test::Run return address. FIX (in `overlay/smtd_full/test_smtd_full.cpp`): add `TestDriver driver;` + `EXPECT_ANY_REPORT(driver).Times(AnyNumber())` to all 3, drop the `DISABLED_`/`_segfaults` names → `mt_overlapped_by_mte`, `mt_plain_interleave`, `lt_hold_with_unmapped_follow_key`. For #3, also map `(1,4,0)=KC_NO` so the fixture doesn't hard-`FAIL()` on a fully-unmapped cell (a real keymap always has KC_NO/KC_TRNS). All 3 now run the full faceroll through sm_td and end with `get_mods()==0` / layer 0 — confirming sm_td handles these multi-key sequences correctly. smtd_full now 19/19 green; whole contour + Python suite (117, 4 known baseline FAILs) unchanged. NOTE: the prior `smtd_apply_stage` SMTD_STAGE_NONE hardening (only unlink when in-stack + `j+1<size`) is a correct defensive fix for a real latent double-removal underflow — KEPT, though it was never the cause of this crash. The lengthy "OPT-DEPENDENT memory corruption in sm_td / re-entrant action_tapping" theory below is DISPROVEN.
- ★ CRASHES FOUND BY FACEROLL (2026-06-14, SUPERSEDED by the RESOLVED note above — these were a harness artifact, not engine bugs) — captured as 3 `DISABLED_` tests in `smtd_full/test_smtd_full.cpp`; sm_td SEGFAULTS through the real pipeline on: (1) `DISABLED_mt_overlapped_by_mte_segfaults` — an SMTD_MT key overlapped by an SMTD_MTE (eager) key: press A(MT), press S(MTE), release A, release S → crash. Two SMTD_MT keys rolled do NOT crash, so the eager mod-tap path is implicated. (2) `DISABLED_mt_plain_interleave_segfaults` — two SMTD_MT keys + plain keys, releasing inner keys while others held (a↓ j↓ b↓ a↑ c↓ b↑ j↑ c↑) → crash; multi-key state-stack mishandling. (3) `DISABLED_lt_hold_with_unmapped_follow_key` — LT hold then a key unmapped on the pushed layer → crash (MAY be a harness artifact: fixture returns KC_TRANSPARENT for unmapped cells; confirm vs a real KC_NO/KC_TRNS keymap). (1) and (2) use fully-mapped real keycodes → unambiguous engine bugs, likely a NULL/stack-index deref in the multi-state path. These are the highest-value output of this work; recommend debugging `smtd_apply_event`/`smtd_apply_stack`/`smtd_create_state` for the >1-active-state path. Run a DISABLED repro with `./.build/test/smtd_full.elf --gtest_also_run_disabled_tests --gtest_filter='*mt_overlapped_by_mte*'` under lldb to get the backtrace.
- CRASH ROOT-CAUSE INVESTIGATION (2026-06-14, PARTIAL — DISPROVEN by the 2026-06-15 RESOLVED note above; kept for the record. The real cause was a dangling host-driver pointer from a missing `TestDriver`, not any sm_td/QMK miscompile): characterized the crashes as an OPT-DEPENDENT memory corruption (passes at -O0, segfaults at the default -O2; ASan/UBSan can't pin it cleanly — ASan reports a wild "DEADLYSIGNAL BUS", i.e. a jump/deref through an already-corrupted pointer). Watchpoint on `idle_for`'s saved return address (arm64 `$fp+8`) shows the saved LR gets overwritten with `testing::Test::Run()+212` (a wrong address) — a STACK return-address smash via a wild write whose target shifts with stack layout (that's why only some tests crash from the same bug; e.g. single-MT `mt_tap` and `roll_two_mt_keys` survive, MT-overlapped-by-MTE and the 3+ key MT+plain interleave die). The corrupting value seen in globals is ~0x0a (= SMTD_POOL_SIZE 10). Tracing `process_smtd`/`smtd_create_state`/`smtd_apply_stage` (build with `-DSMTD_DEBUG_ENABLED` or ad-hoc `fprintf(stderr,...)`, `setbuf(stdout,NULL)` + `-Wno-error=format`) shows only the FIRST key reaches `process_smtd`; the second key never does, i.e. the smash happens during the first key's scan and breaks subsequent scanning. NOT an uninitialized state field (EMPTY_STATE/reset_state cover all), NOT MAX_DEFERRED_EXECUTORS overflow (default 8). PARTIAL FIX SHIPPED in sm_td.c: `smtd_apply_stage` SMTD_STAGE_NONE branch now only unlinks when the state is actually in the active stack (`state->idx < size && smtd_active_states[state->idx] == state`) and uses `j + 1 < size` to avoid the unsigned-underflow that made `smtd_active_states_size--` wrap to 255 and `smtd_active_states[255]=NULL` write OOB — a real latent double-removal bug, but it did NOT resolve these particular segfaults (they persist), so the primary wild-write site is still elsewhere in the >1-state path. DEEPER INVESTIGATION (2026-06-14/15, interactive) — narrowed but NOT fixed. Decisive optimization-bisection: whole-build `-O0` PASSES (no crash); `-O0` only on sm_td.c (rest `-O2`) CRASHES; whole `-O2` CRASHES. => the miscompiled/UB code is NOT in sm_td.c — it's in the NON-sm_td code (QMK quantum/action_tapping/action or the gtest harness) at `-O2`, triggered by sm_td's behavior. Sanity checks injected into sm_td (state pointer in-pool, idx<POOL, size<=POOL at handle_action/apply_to_stack/reapply-loop) do NOT fire at `-O0` => sm_td's own bookkeeping is internally consistent; no garbage state pointer / bad idx inside sm_td. Localized trigger: `roll_two_mt_keys` (MT+MT overlap) is CLEAN under ASan, but MT-overlapped-by-MTE and a 3+ key MT+plain interleave crash — i.e. the bug needs ≥2 tap-hold states overlapping while uncertain, with the DEFERRED-ACTION RE-APPLICATION path (sm_td.c smtd_handle_action tail loop, line ~674: when an earlier state resolves it replays following states' deferred actions). Working theory: sm_td RE-ENTERS QMK's `process_record`/`action_tapping` via `smtd_emulate_key` while emitting during that re-application; QMK's action_tapping uses non-reentrant static state (`tapping_key`/waiting buffer) and miscompiles/corrupts at `-O2` for this specific overlap timing (the corrupted value lands on a return address / code pointer, which is why ASan/UBSan only see a downstream wild jump "DEADLYSIGNAL BUS", not a clean OOB). Crash surfaces as `idle_for`→`keyboard_task`'s call chain returning into garbage (gtest `Test::Run`/`OsStackTraceGetter` vtable). DISPROVEN theories (tested): (a) `NO_ACTION_TAPPING` in the suite config does NOT fix the crash — so it is NOT action_tapping re-entrancy. (b) The "sm_td.c-only -O0 still crashes" result is UNRELIABLE (`#pragma clang optimize off` may not have applied), so it does NOT prove the UB is outside sm_td.c. Solid facts only: whole-build `-O0` PASSES, whole `-O2` CRASHES, `-O0`+ASan CRASHES (BUS, no clean report), and sm_td-internal sanity checks (state-in-pool / idx / size) never fire — so whatever is wrong is invisible to those checks. NEXT: per-translation-unit opt bisection done properly (e.g. a make rule forcing -O0 on individual files: sm_td.o, then quantum/action.c, process_record path, keyboard.c) to definitively localize the miscompiled/UB TU; then assembly/-fsanitize=undefined per-file. Substantial; not yet done.
Next step (original note) requires INTERACTIVE lldb single-stepping; deferred. The 3 `DISABLED_*` tests in smtd_full are the regression net for when it's fixed. Useful one-liner: build smtd_full with `EXTRAFLAGS += -g -fsanitize=address` in its test.mk, then `lldb -b -o run -o "bt all" .build/test/smtd_full.elf` with `GTEST_ALSO_RUN_DISABLED_TESTS=1 GTEST_FILTER=...`. (2) OPEN BEHAVIOR FINDING: a LONE long press of a raw `MT()` under SMTD_ENABLE_QMK_TAPHOLD resolves to a TAP, not a held mod — hold is committed by an overlapping following key, not the touch timeout alone. The full-engine `SMTD_MT` hold+following-key DOES register the mod. Diverges from the Python mock (where `prolong()` forces a hold); investigate on hardware. (3) `LAYER_PUSH` (SMTD_LT hold) is not reflected by `layer_state_is(layer)` even though the pushed layer is active for keycode resolution — tests assert layer behavior via the emitted keycode (cell resolves to the layer-1 keycode) instead of `layer_state_is`.
- sm_td_minimal module REMOVED (2026-06-15, never released): the `sm_td_minimal/` dir and the `smtd_minimal` QMK-native suite were deleted — "not needed in this form right now". The pre_process_record design idea (a minimal MT/LT-only module that READs on pre_process_record before action_tapping and EMITs via process_record) is still recorded in the CAVEAT note above for if/when it's revisited. The earlier (now-removed) module: own dir/qmk_module.json (module SM_TD_MINIMAL), `sm_td_minimal.c` did `#include "../sm_td/sm_td.c"`, weak `on_smtd_action` default + `pre_process_record_sm_td_minimal` calling `process_smtd`, `config.h` forced SMTD_ENABLE_QMK_TAPHOLD=1 + MAX_DEFERRED_EXECUTORS=10. It built/linked on zsa/voyager but was never hardware-behavior-tested. Historical smtd_minimal suite counts in the 2026-06-13/14 harness notes above are pre-removal.

## Commit & Pull Request Guidelines
- Commits: concise, imperative; use conventional types (`feat:`, `fix:`, `docs:`, `test:`, `refactor:`, `build:`, `chore:`). Example: `feat(mt): eager mod-tap handling`.
- PRs include: linked issue, rationale, before/after behavior, updated docs (if user-facing), and test commands/suites run locally.
- Always run `python3 tests/run_tests.py` before submitting.

## Release Process
- Cutting a release is one `release vX.Y.Z` commit on `main`. Bump `Version:`/`Date:` in all three of `sm_td/sm_td.c`, `sm_td/sm_td.h`, `sm_td/introspection.h` (kept in lockstep); prepend a changelog entry to `docs/015_releases.md`; in `README.md` move the `(we are here)` marker to the new version and add its bullet list (0.6.x block is newest-first). Feature/fix work lands first on a `release-X.Y.Z` branch, then the release commit is added and the branch fast-forwards into `main`.
- Publish: `git tag -a vX.Y.Z` on the release commit + `git push origin main vX.Y.Z`, then `gh release create vX.Y.Z --latest --notes-file <body> sm_td/sm_td.c sm_td/sm_td.h` (GitHub releases attach `sm_td.c` and `sm_td.h` as assets — see v0.6.1/v0.6.2 for the body format: "What's new" + "Upgrade from" sections, ending with a link to `docs/015_releases.md`).

## Configuration Tips
- In QMK: set `DEFERRED_EXEC_ENABLE = yes` and define `MAX_DEFERRED_EXECUTORS` per docs.
- Tune via `SMTD_GLOBAL_*` and `on_smtd_action(...)`; document per-key overrides in `docs/`.
