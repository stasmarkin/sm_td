#pragma once

#include "test_common.h"

/* Handle standard QMK MT()/LT() keycodes with sm_td timing, plus the
 * chordal-hold (opposite-hands) rule. */
#define SMTD_ENABLE_QMK_TAPHOLD 1
#define SMTD_CHORDAL_HOLD 1
